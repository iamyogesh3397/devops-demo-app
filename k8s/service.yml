apiVersion: v1
kind: Service
metadata:
  name: devops-demo-app-service
spec:
  type: NodePort
  selector:
    app: devops-demo-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
